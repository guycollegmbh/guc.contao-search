<div class="tl_listing_container">

    <h2>Index-Status</h2>
    <table class="tl_listing">
        <thead>
            <tr>
                <th>Typ</th>
                <th>Einträge</th>
                <th>Zuletzt indexiert</th>
            </tr>
        </thead>
        <tbody>
            {% for type, count in stats %}
            <tr class="{{ loop.index is odd ? 'odd' : 'even' }}">
                <td>
                    <strong>{{ type|capitalize }}</strong>
                    {% if type not in indexerTypes %}<span style="color:#999; font-size:11px"> (Kategorie)</span>{% endif %}
                </td>
                <td>{{ count }}</td>
                <td>
                    {% if lastIndexed[type] is defined and lastIndexed[type] is not null %}
                        {{ lastIndexed[type]|date('d.m.Y H:i') }}
                    {% elseif type not in indexerTypes and lastIndexed['page'] is defined and lastIndexed['page'] is not null %}
                        {{ lastIndexed['page']|date('d.m.Y H:i') }}
                    {% else %}
                        —
                    {% endif %}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <p style="margin-top:16px; color:#999">
        SQLite-Datenbankgrösse: {{ dbSize }} KB &nbsp;·&nbsp;
        Re-Indexierung erfolgt automatisch bei Backend-Änderungen und via Cron.
    </p>

</div>
